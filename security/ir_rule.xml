<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="rule_tablero_tarea_usuario" model="ir.rule">
            <field name="name">Tareas Personales: Solo Responsable</field>
            <field name="model_id" ref="model_tablero_tarea"/>
            <field name="groups" eval="[(4, ref('tablero_kanban.group_tablero_user'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="rule_tablero_tarea_gerente" model="ir.rule">
            <field name="name">Gestión Total: Gerente y Admin</field>
            <field name="model_id" ref="model_tablero_tarea"/>
            <field name="groups" eval="[(4, ref('tablero_kanban.group_tablero_manager')), (4, ref('tablero_kanban.group_tablero_admin'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field> </record>

    </data>
</odoo>