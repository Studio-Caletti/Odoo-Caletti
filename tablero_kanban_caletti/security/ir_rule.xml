<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- REGLA 1: Usuarios Base - Solo ven sus propias tareas -->
        <record id="rule_tablero_tarea_usuario" model="ir.rule">
            <field name="name">Tareas Personales: Solo Responsable</field>
            <field name="model_id" ref="model_tablero_tarea"/>
            <field name="groups" eval="[(4, ref('tablero_kanban_caletti.group_tablero_user'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- REGLA 2: Gerentes y Administradores - Ven todo -->
        <record id="rule_tablero_tarea_gerente" model="ir.rule">
            <field name="name">Gestión Total: Gerente y Admin</field>
            <field name="model_id" ref="model_tablero_tarea"/>
            <field name="groups" eval="[(4, ref('tablero_kanban_caletti.group_tablero_manager')), (4, ref('tablero_kanban_caletti.group_tablero_admin'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

        <!-- REGLA 3: Portal - Solo ven tareas de su empresa/cliente (NUEVA) -->
        <record id="rule_tablero_tarea_portal" model="ir.rule">
            <field name="name">Portal: Ver solo tareas de mi cliente</field>
            <field name="model_id" ref="model_tablero_tarea"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="domain_force">[
                '|',
                ('partner_id', '=', user.partner_id.id),
                ('partner_id', '=', user.partner_id.commercial_partner_id.id)
            ]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

    </data>
</odoo>
